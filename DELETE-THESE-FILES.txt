Phase 11.4 delivery — files to delete from the working copy
============================================================

A zip can't represent a deletion, so remove these seven items manually
in GitHub Desktop (or `git rm`) after dropping in this zip's contents:

  DELETE-THESE-FILES.txt   -> already-applied Phase 4.1.3 manifest, dead
  DELETE_THESE_FILES.txt   -> already-applied Phase 5.1 manifest, dead
  APPLY-THIS-ZIP.txt       -> already-applied theme-bug manifest, dead
  CHANGES.txt              -> already-applied Phase 9.3 manifest, dead
  README-DEPLOY.txt        -> already-applied Phase 4.3 manifest, dead
  Data/                    -> superseded logo-exploration source (unreferenced
                              anywhere in the codebase; public/brand/source/
                              already holds the real reference file)

(Yes, this delivery's own DELETE-THESE-FILES.txt asks you to delete a
file by the same name — that's the old one from Phase 4.1.3. This one
can go too, right after you're done reading it.)

Everything else in this zip is an in-place edit to an existing path —
drop it into the repo root and overwrite.

See docs/phases/PHASE-11.4-README.md for the full list of what changed
and why.
