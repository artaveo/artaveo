Phase 9.3 — Notifications — file manifest
==========================================
Drop these into your local working copy at the same paths. Nothing needs
deleting for this phase.

NEW FILES
---------
db/migrations/0002_notification_outbox.sql   (already applied to the live Supabase project — included for your repo history/rollback reference)
lib/notifications/provider.ts
lib/notifications/templates.ts
lib/notifications/outbox.ts
app/api/cron/notifications/route.ts
vercel.json
docs/phases/PHASE-9.3-README.md

MODIFIED FILES
--------------
app/actions/inquiries.ts   — enqueues + best-effort sends the two notifications after a successful insert
.env.example               — documents EMAIL_PROVIDER / RESEND_API_KEY / EMAIL_FROM_ADDRESS / CRON_SECRET
lib/site.ts                — one comment fixed (stale "Phase 9.4" reference corrected to "§ 9.3")
ROAD-MAP-ARTAVEO.md        — Phase 9.3 marked PARTIAL (structure complete, sending inactive pending D-01), D-01 row updated, phase index updated

STATUS: PARTIAL — see docs/phases/PHASE-9.3-README.md for the full reasoning.
Real e-mail sending stays off (console/no-op provider) until you buy the
domain and D-01 resolves. When that happens, flip it on with three env
vars only — no code change:

  EMAIL_PROVIDER=resend
  RESEND_API_KEY=<from Resend>
  EMAIL_FROM_ADDRESS=<address on your new, DNS-authenticated domain>

Nothing else in the repo was touched.
