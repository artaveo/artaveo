# Roadmap § 9.2 — Server handling & persistence.
#
# D-10 is resolved: the live `Artaveo` Supabase project exists
# (ukzovqnpqjcrwofycalc, ap-southeast-1, free plan). SUPABASE_URL below
# is filled in since it's public info; SUPABASE_SERVICE_ROLE_KEY is left
# blank on purpose — copy it yourself from the Supabase dashboard
# (Settings → API → service_role) into your own .env.local (already
# gitignored). Never commit the real value — it bypasses Row Level
# Security entirely.

SUPABASE_URL=https://ukzovqnpqjcrwofycalc.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Used only to salt the IP hash stored for rate-limiting (§ 9.2: "without
# extra personal data" — the raw IP itself is never stored). Any random
# string; rotate it and old hashes simply stop matching, which is fine.
INQUIRY_IP_HASH_SECRET=

# Roadmap § 9.3 — Notifications.
#
# D-01 (production domain + SPF/DKIM/DMARC) is still open, so real
# sending is OFF by default: leave EMAIL_PROVIDER unset (or 'console') and
# every notification is logged instead of sent, while still going through
# the full outbox/retry pipeline honestly. Once the real domain is bought
# and DNS-authenticated (D-01), set all three of the following to switch
# on real sending — no code change needed:
#
#   EMAIL_PROVIDER=resend
#   RESEND_API_KEY=<from the Resend dashboard>
#   EMAIL_FROM_ADDRESS=<an address on the authenticated domain, e.g. hello@artaveo.dev>
#
# CRON_SECRET is auto-provisioned by Vercel for the scheduled sweep in
# vercel.json (app/api/cron/notifications) — do not set it yourself in
# Vercel; only needed locally if you want to call that route by hand.
EMAIL_PROVIDER=console
RESEND_API_KEY=
EMAIL_FROM_ADDRESS=
CRON_SECRET=
